# Workflow 文件与链接验收（2026-09-07）

聊天与 Workflow 共用项目文件栏。Workflow 对话、规划说明和节点结果复用项目 Markdown 导航；节点产物直接打开文件栏。文件刷新按运行 ID／序号去重并合并到最多每两秒一次，退出会话取消延迟刷新。已关闭的 SSE 连接在快照轮询恢复后重新建立。

## 自动检查

- `npm test`：208 项，207 通过、1 跳过、0 失败。8080 已被现有服务占用，端口冲突 fixture 按原有规则跳过。
- `npm run typecheck`、`npm run lint`、`npm run build`、`git diff --check` 通过。
- 新增测试覆盖首次快照、未变化轮询、连续序号变化合并、末次延迟刷新、重连序号跳跃、新运行、退出取消，以及公共侧栏和 Markdown 导航接线。

## 浏览器验收

使用 `node scripts/workflow-file-ui-fixture.mjs`，默认独立端口 3017（可用 `WORKFLOW_UI_PORT` 覆盖），两个临时项目及独立 Runtime 数据库。Workflow 消息为明确标注的模拟结果，文件读取／修改、预览接口和 SSE 使用真实本地实现；未调用真实模型。

- 桌面：右栏显示、折叠、文件链接自动展开、拖动宽度通过；管理页保持原布局，返回恢复 Workflow。
- 链接：对话与规划中的中文空格文件名、文档内相对附录、代码 `#L60` 定位通过。浏览器计算链接颜色为 `rgb(57, 118, 197)`。
- 节点：产物按钮及结果链接打开同一文件栏，详情弹窗消失，焦点落到 `workspace-file-tabpanel`；网页链接打开新标签并保留详情。
- 文件：GFM 表格、代码高亮和行号定位通过；缺失文件显示错误及“重新加载”入口。
- 状态：同项目切换聊天／Workflow 保留标签，A／B 项目的同名报告显示各自内容，切回 A 恢复 A 的标签。
- 更新：`update` 写入报告新版本及新增文件，目录和已打开预览自动更新；展开的 reports 目录保持展开。`complete` 后可见最终版本。
- 断线：`offline` 后保留旧目录；离线期间 `update`，再 `online` 后自动读取新版本，SSE 重建，连接错误提示清除。
- 窄屏：390×844 下，顶栏可打开项目文件抽屉，关闭按钮正常；从节点结果打开代码后关闭详情、展开文件抽屉并聚焦预览。

fixture 支持 stdin 命令 `update`、`complete`、`offline`、`online`、`stats`、`quit`；退出时清理临时项目和数据库。
