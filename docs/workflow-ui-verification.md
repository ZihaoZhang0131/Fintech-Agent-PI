# Workflow 对话重构验收（2026-09-06）

## 自动化

- 完整测试：169 项通过。
- TypeScript、ESLint、生产构建、`git diff --check` 通过。
- 新增对话测试覆盖：并发发送去重、同一对话多轮运行、前文结果上下文、活动运行反馈只追加一次、重启后消息 ID 稳定、旧运行映射。
- 现有测试继续覆盖 DAG 调度、恢复与幂等写入、停止迟到结果、版本冲突、模板跨项目映射、SSE 游标恢复、聊天键盘交互。

## 浏览器交互

| 场景 | 验收结果 |
| --- | --- |
| 对话首页 | 共用项目侧栏；顶部标题与模式切换；输入框始终可见；资产、设置、Trace 不常驻 |
| 旧运行 | 原美团研究运行打开为用户消息、规划 DAG、补充信息及恢复状态 |
| 两轮真实模型 | 第一轮两个并行计算节点加汇总得到 35；第二轮在原对话沿用结果得到 70；旧规划自动折叠 |
| 暂停与继续 | 已暂停运行的主按钮为继续；继续后重新规划执行 |
| 停止与刷新 | 在 12 节点验收运行的最后规划阶段停止；刷新后仍为已停止，没有迟到总结；草稿保留 |
| 大型 DAG | 12 节点紧凑图有高度限制；展开后可选版本、缩放；适应视图同时计算宽高，513px 容器没有纵向溢出 |
| 节点详情 | 临时抽屉显示任务、标准、完整结果、尝试、输入与 Trace；活动节点可打开编辑表单 |
| 管理返回 | 从节点进入 Trace、从对话进入设置或模型页，返回原 Workflow 对话；草稿不丢失 |
| 跨项目模式切换 | Workflow 切换到另一项目后，聊天恢复自己的项目和会话；再回 Workflow 及刷新仍保留对应草稿 |
| 窄屏 | 390×844 下，消息与输入框独立布局；节点抽屉占用完整宽度；Escape 关闭后恢复焦点 |
| 审批 | 隔离 fixture 中实际批准只打印标记的 Bash 命令；批准／拒绝显示在执行消息内，完成后消失 |
| 长回答 | 隔离 fixture 的 40 段模拟 Markdown 可滚动；向上滚动到 381px 后，后台刷新和输入草稿仍保持 381px |
| 模板 | 在隔离环境从规划保存模板，在独立资产页复制、归档及查看归档状态；参数／角色映射表单仍保留 |
| 键盘 | Enter 发起实际任务；Shift+Enter 产生换行，中文草稿正常保留；输入法组合状态、229 键值保护由自动化测试覆盖 |

浏览器刷新覆盖 SSE 断开与重新订阅；指定旧游标重放及并发重试由接口测试覆盖。跨项目角色能力验证和节点修改后的下游失效由后端测试覆盖。未手动切换 macOS 输入法候选窗口。

## 真实工具链

额外运行 `scripts/workflow-smoke.mjs --live`：DeepSeek V4 Pro、3 个 Subagent 节点、4 次实际项目文件读取、6 条 Trace；最终汇总 70 万元。创建去重和模板保存通过。临时项目及凭据副本已清理；无密钥摘要见 `.local-data/workflow-smoke-latest.json`。

`workflow-ui-fixture.mjs` 使用独立临时数据库及端口，审批调用真实本机命令管理器；长回答明确标为模拟内容，不将它作为真实模型结果。
